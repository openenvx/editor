/**
 * Published fat-bundle entry. Source of truth for the host API is
 * `@openenvx/html-studio` (workspace TypeScript). This package inlines that
 * surface plus html/workbench deps into `dist/` for GitHub Packages.
 */
export * from '@openenvx/html-studio';
