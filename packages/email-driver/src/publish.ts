/** Public npm surface — keep in sync with `apps/email-package-demo`. */
export { defaultEmailWorkbench } from './default-email-workbench';
export {
  createEmailScene,
  renderEmailHtml,
  type RenderEmailHtmlOptions,
  type Scene,
} from './publish-runtime';
