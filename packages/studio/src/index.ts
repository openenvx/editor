/**
 * Published fat-bundle entry. Source of truth for the host API is
 * `@openenvx/canvas-studio` (workspace TypeScript). This package inlines that
 * surface plus canvas/workbench deps into `dist/` for GitHub Packages.
 */
export * from '@openenvx/canvas-studio';
