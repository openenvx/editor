/** @deprecated Import from `./document-store` instead. */
export {
  DocumentStore as SceneStore,
  moveNodeToIndex as moveLayerToIndex,
  reorderNodes as reorderLayers,
  type PageRulesLookup,
} from './document-store';
