/** Optional visibility for a layout row, block, or input group. */
export interface PropertyLayoutWhenOptions {
  /** When-expression: bare tokens = context keys; `$` + path = live property data. */
  when?: string;
}
