# Snapvelo Event Page Demo

Locked event-page designer demo for Snapvelo on top of `@openenvx/html` (unchanged). Organizers edit content and theme tokens — not freeform block order, page layout, or hero variant.

```bash
bun run dev:snapvelo
```

Preview + secondary sidebar (Inspector). Activity bar and primary sidebar are off.
