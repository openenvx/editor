# Snapvelo Event Page Demo

Locked event-page designer demo for Snapvelo. Blocks and plugin live in `@openenvx/snapvelo`; the shell comes from `@openenvx/html-studio`.

```bash
bun run dev:snapvelo
```

Preview + secondary sidebar (Inspector). Activity bar and primary sidebar are enabled in `app.tsx` for debugging.
