{
  "name": "@openenv/openenvx",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "build": "turbo run build",
    "test": "turbo run test -- --run",
    "bench": "turbo run bench --filter=@openenvx/core --filter=@openenvx/canvas",
    "lint": "turbo run lint",
    "check": "ultracite check && knip",
    "check-types": "turbo run check-types --filter='!./apps/*'",
    "precommit": "bun run check && turbo run check-types --filter='...[HEAD]' --filter='!./apps/*'",
    "knip": "knip",
    "fix": "ultracite fix",
    "changeset": "changeset",
    "version-packages": "changeset version && bun update",
    "release:changeset": "bun run build && bun run publish-packages && changeset tag",
    "publish-packages": "bun run publish-package:extensions && bun run publish-package:html-studio && bun run publish-package:email-studio && bun run publish-package:canvas-studio",
    "publish-studio-packages": "bun run publish-package:html-studio && bun run publish-package:email-studio && bun run publish-package:canvas-studio",
    "version:studios": "bun scripts/release-studios.ts --bump-only",
    "release:studios": "bun scripts/release-studios.ts",
    "publish-package:extensions": "cd packages/extensions && bun pm pack && tgz=$(ls -t *.tgz | head -1) && npm publish \"$tgz\" && rm -f \"$tgz\"",
    "publish-package:html-studio": "bun scripts/publish-npm-public.ts packages/html-studio",
    "publish-package:email-studio": "bun scripts/publish-npm-public.ts packages/email-studio",
    "publish-package:canvas-studio": "bun scripts/publish-npm-public.ts packages/canvas-studio",
    "link:extensions": "cd packages/extensions && bun link",
    "link:html-studio": "cd packages/html-studio && bun link",
    "verify-pack": "bun run verify-pack:extensions && bun run verify-pack:html-studio && bun run verify-pack:email-studio && bun run verify-pack:canvas-studio",
    "verify-pack:extensions": "bun run build --filter=@xmazu/openenvxee-extensions && cd packages/extensions && bun pm pack && tgz=$(ls -t *.tgz | head -1) && echo \"==> packages/extensions ($tgz)\" && tar -xOf \"$tgz\" package/package.json | node -e \"const d=JSON.parse(require('fs').readFileSync(0,'utf8')); const exp=d.exports?.['.']; const def=typeof exp==='string'?exp:exp?.default; if(!def?.includes('dist/')){console.error('Published export must point at dist, got:',exp);process.exit(1)} if(!d.exports?.['./protocol']){console.error('missing ./protocol export');process.exit(1)} if(!d.files?.includes('dist')){console.error('files must include dist');process.exit(1)} console.log('extensions pack ok')\" && rm -f *.tgz",
    "verify-pack:html-studio": "bun run build --filter=@openenvx/html-studio && bun packages/html-studio/verify-pack.ts",
    "verify-pack:email-studio": "bun run build --filter=@openenvx/email-studio && bun packages/email-studio/verify-pack.ts",
    "verify-pack:canvas-studio": "bun run build --filter=@openenvx/canvas-studio && bun packages/canvas-studio/verify-pack.ts",
    "dev:playground": "turbo run dev --filter=demo-playground",
    "dev:canvas": "turbo run dev --filter=@openenvxee/canvas-demo",
    "dev:html": "turbo run dev --filter=@openenvxee/html-demo",
    "dev:email": "turbo run dev --filter=@openenvxee/email-demo",
    "dev:email-package": "bun run build --filter=@openenvx/email-studio && turbo run dev --filter=@openenvxee/email-package-demo",
    "dev:canvas-package": "bun run build --filter=@openenvx/canvas-studio && turbo run dev --filter=@openenvxee/canvas-package-demo",
    "dev:html-package": "bun run build --filter=@openenvx/html-studio && turbo run dev --filter=@openenvxee/html-package-demo"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "eslint-plugin-github": "6.1.2",
    "eslint-plugin-sonarjs": "^4.2.0",
    "husky": "^9.1.7",
    "knip": "^6.32.3",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "oxlint-plugin-react-doctor": "^0.9.12",
    "turbo": "^2.10.12",
    "typescript": "catalog:dev",
    "ultracite": "7.10.6"
  },
  "catalogs": {
    "dev": {
      "typescript": "^6.0.3",
      "tsup": "^8.5.0"
    },
    "testing": {
      "vitest": "^4.1.10",
      "@vitest/coverage-v8": "^4.1.10"
    }
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "bun@1.3.0"
}