#!/usr/bin/env sh
set -e

cd -- "$(git rev-parse --show-toplevel)"

# Auto-format, stage formatter fixes, then run the same gates as `bun run precommit`.
# Skip locally: HUSKY=0 git commit ...
bun run fix
git add -u
bun run precommit
